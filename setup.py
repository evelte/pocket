from setuptools import setup

setup(name='pocket_reading',
      version='v1',
      url='http://pocket_reading.org',
      description='',
      author='',
      license='',
      packages=[],
      zip_safe=False,
      install_requires=[])
