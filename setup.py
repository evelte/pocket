from setuptools import setup

setup(name='pocket_reading',
      version=version,
      url='http://pocket_reading.org',
      description='',
      author='',
      license='',
      packages=[],
      zip_safe=False,
      install_requires=[])
